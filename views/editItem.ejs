<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Item</title>
    <p style="font-size: 2em; font-weight: bold">
      Make changes to the required fields:
    </p>
    <p style="font-size: 2em">
      Leave unchanged if no particular changes for that value
    </p>
    <p>Item ID:<%=item.product_id%></p>
    <p>Item Original Name:<%=item.product_name%></p>
    <hr />
    <form method="post" action="/editProduct/<%=item.product_id%>">
      <label for="newName">Enter new name:</label>
      <input type="text" name="newName" value="<%=item.product_name%>" /><br />
      <label for="newDescription">Enter new description:</label>
      <input
        type="text"
        name="newDescription"
        value="<%=item.description%>"
      /><br />
      <label for="newPrice">Enter new description:</label>
      <input
        type="number"
        step="0.01"
        name="newPrice"
        value="<%=item.price%>"
      /><br />
      <label for="newQuantity">Enter new quantity:</label>
      <input
        type="number"
        step="1"
        name="newQuantity"
        value="<%=item.quantity%>"
      /><br />
      <button
        type="submit"
        onclick="confirm('Do you want to make these changes?')"
      >
        Submit
      </button>
    </form>
  </head>
  <body></body>
</html>
